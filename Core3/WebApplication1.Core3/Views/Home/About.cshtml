@{
    ViewData["Title"] = "關於";
}

<main aria-labelledby="title">
    <h2 id="title">@ViewData["Title"]</h2>
    <p>此頁面由原本的 <code>About.aspx</code> 遷移而來。</p>
    <p>
        在 ASP.NET Core MVC（VB.NET）中，頁面內容由 Razor View 與 Layout 組合，
        不再依賴 Web Forms Master Page 與 code-behind 事件（<code>Page_Load</code>）。
        本頁對應 <code>HomeController.vb</code> 的 <code>About()</code> Action。
    </p>
</main>
